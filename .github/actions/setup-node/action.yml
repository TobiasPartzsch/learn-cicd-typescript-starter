name: 'Setup Node + Install'
description: 'Checkout code, set up Node v18, and install dependencies'

runs:
  using: "composite"
  steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-node@v4
      with:
        node-version: 18
    - run: npm ci
      shell: bash